import { useAuth } from '../auth/useAuth'
export default function AuthHomePage() { const { user, logout } = useAuth(); return <main className="min-h-screen bg-slate-950 px-6 py-16 text-slate-100"><section className="mx-auto max-w-2xl rounded-xl border border-slate-800 bg-slate-900 p-8"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-sky-400">Authenticated</p><h1 className="mt-3 text-3xl font-bold">Welcome, {user.email}</h1><p className="mt-4 text-slate-300">Role: <span className="font-semibold text-slate-100">{user.role === 'MANAGER' ? 'Sales Manager' : 'Sales Rep'}</span></p><p className="mt-2 text-sm text-slate-400">{user.role === 'MANAGER' ? 'Manager-only server routes are available for this account.' : 'Your sales-rep session is active.'}</p><button className="mt-8 rounded-md border border-slate-600 px-4 py-2 text-sm font-semibold hover:bg-slate-800" type="button" onClick={logout}>Log out</button></section></main> }
