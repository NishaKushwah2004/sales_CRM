import { Link, useParams } from 'react-router-dom'
import { useEffect, useState } from 'react'
import api from '../api/client'
import { useAuth } from '../auth/useAuth'
export default function CompanyDetailPage() { const { id } = useParams(); const { user } = useAuth(); const [company, setCompany] = useState(null); const [form, setForm] = useState(null); const [error, setError] = useState(''); useEffect(() => { api.get(`/companies/${id}`).then((response) => { setCompany(response.data.data.company); setForm(response.data.data.company) }).catch((requestError) => setError(requestError.response?.data?.error?.message || 'Unable to load company.')) }, [id]); async function save(event) { event.preventDefault(); try { const response = await api.patch(`/companies/${id}`, { name: form.name, industry: form.industry, website: form.website }); setCompany(response.data.data.company); setForm(response.data.data.company) } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Unable to update company.') } } async function changeArchive(action) { try { const response = await api.post(`/companies/${id}/${action}`); setCompany(response.data.data.company); setForm(response.data.data.company) } catch (requestError) { setError(requestError.response?.data?.error?.message || 'Unable to update archive status.') } } if (error) return <main className="min-h-screen bg-slate-950 p-12 text-red-200">{error}</main>; if (!company || !form) return <main className="min-h-screen bg-slate-950 p-12 text-slate-100">Loading company...</main>; return <main className="min-h-screen bg-slate-950 px-6 py-12 text-slate-100"><div className="mx-auto max-w-2xl"><Link className="text-sky-400" to="/companies">Back to companies</Link><h1 className="mt-4 text-3xl font-bold">{company.name}</h1><p className="mt-2 text-sm text-slate-400">Owner: {company.owner.email}</p>{!company.archivedAt && <form className="mt-8 rounded border border-slate-800 bg-slate-900 p-6" onSubmit={save}>{['name','industry','website'].map((field) => <label className="mt-3 block text-sm" key={field}>{field[0].toUpperCase() + field.slice(1)}<input className="mt-1 w-full rounded border border-slate-700 bg-slate-950 p-2" value={form[field]} onChange={(event) => setForm({ ...form, [field]: event.target.value })} required /></label>)}<button className="mt-5 rounded bg-sky-500 px-4 py-2 font-semibold text-slate-950">Save changes</button></form>}{user.role === 'MANAGER' && <button className="mt-4 rounded border border-red-500 px-4 py-2 text-red-200" onClick={() => changeArchive(company.archivedAt ? 'restore' : 'archive')}>{company.archivedAt ? 'Restore company' : 'Archive company'}</button>}</div></main> }
